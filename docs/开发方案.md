# 槐芽网络训练营 · 开发方案

> 需求来源：[小学生电子产品与网络文化安全教育闯关小游戏（网页版）完整需求方案](./小学生电子产品与网络文化安全教育闯关小游戏（网页版）完整需求方案.md)
> 本文档记录 2026-08 需求评审（grilling）后确定的全部技术决策与实施计划。

## 一、项目定位

面向小学生的轻量益智闯关网页游戏，浏览器直接打开即玩，免注册。以趣味闯关、图文答题为核心形式，帮助小学生建立正确的电子产品与网络使用认知。核心宗旨：**不禁止用网、不抵制电子产品，学会择优使用、理性把控、主动避害**。

## 二、已确定的核心决策

| 维度 | 决策 |
|---|---|
| 技术栈 | Vite + TypeScript + 原生 DOM（无框架），题库 JSON 数据驱动，产物纯静态 |
| 页面流程 | 首页 → 闯关大厅（顺序解锁）→ 答题 → 单关结算+知识小结 → 勋章中心 → 通关证书 |
| 关卡结构 | 4 主题关（各 10 题）顺序解锁 → 终极关（40 题池随机抽 10 题） |
| 题型 | 判断 / 单选 / 多选，全部点击操作；多选题带「确认」按钮 |
| 判定规则 | 答错即讲当题放行、零失败感；每题 1 分（多选全部选对才得分，漏选/多选/错选均按答错走解析）；通关即得勋章 |
| 配图 | 40 张题目主图均为 **AI 生成 WebP**（`assets/images/q{关}-{题}.webp`，1152×864 · q82，由 `scripts/compress-images.mjs` 从 2304×1728 源 PNG 压缩）。数据层不存扩展名，import.meta.glob 前缀解析，**webp → png → svg 依次优先**；源 PNG 仅存本地 `assets/images-raw/`（不入库不进包）。反馈环节不单独配图，用 CSS 动画 + 表情图标 + 题目原图复用 |
| 语音朗读 | **砍掉**（需求文档中该功能取消），界面不保留语音开关 |
| 音频 | BGM / 答题音效全部 WebAudio 代码合成，无音频资源文件；界面保留一个音效总开关 |
| 证书 | Canvas 绘制「青少年健康上网小能手」电子证书，输入昵称（默认 **小槐芽**，可跳过），一键下载 PNG |
| 进度保存 | localStorage：通关进度、勋章、积分、设置项，下次打开可继续 |
| 部署 | GitHub（public）+ Vercel，域名 `huaiya.535334587.xyz`（Cloudflare 子域）。**仅线上部署，不做离线版本** |
| 构建 | `base: './'`，hash 路由，普通 Vite 构建即可 |
| 多端适配 | 响应式，移动优先布局，适配电脑 / 平板 / 手机浏览器 |

## 三、页面与路由

单页应用，hash 路由（纯静态托管友好，无需服务端重写配置）：

| 路由 | 页面 | 说明 |
|---|---|---|
| `#/` | 首页 | 游戏标题、卡通主视觉、「开始闯关」按钮、音效开关入口 |
| `#/hall` | 闯关大厅 | 4 主题关 + 终极关入口卡片，未解锁置灰，显示各关进度/勋章 |
| `#/level/:id` | 答题页 | 统一版式：顶部关卡标题+趣味小标题 / 中间场景配图 / 题目文字 / 底部选项按钮 |
| `#/result/:id` | 单关结算 | 积分、答对题数、专属勋章获得动画、本关知识小结 |
| `#/medals` | 勋章中心 | 5 枚勋章收集状态、总积分展示 |
| `#/cert` | 通关证书 | 四关+终极关全通后解锁；昵称输入（默认小槐芽）、Canvas 证书预览、下载 PNG |

## 四、数据模型

### 4.1 题库 `src/data/questions.ts`

```ts
type QuestionType = 'judge' | 'single' | 'multi'

interface Question {
  id: string                 // 如 'q1-1'（关卡1第1题）
  levelId: 1 | 2 | 3 | 4     // 所属主题关
  type: QuestionType
  image: string              // 配图路径 assets/images/q1-1.webp
  text: string               // 题目原文
  options: string[]          // 选项（判断题为 ['正确','错误']）
  answer: number[]           // 正确答案索引数组（多选多个）
  explain: string            // 答错时的通俗解析话术
}
```

- 题目内容 100% 来自需求文档第十一章，共 40 题（每关 10 题）。
- 终极关从 40 题池 `shuffle()` 随机抽 10 题，沿用原题与配图。

### 4.2 关卡 `src/data/levels.ts`

```ts
interface Level {
  id: number                 // 1-4 为主题关，5 为终极关
  name: string               // 如「网络宝藏甄别师」
  subtitle: string           // 趣味小标题
  emoji: string              // 大厅卡片/勋章图标（占位阶段用 emoji，勋章正式图后补可选）
  medal: string              // 勋章名：甄别师/清除官/小达人/安全卫士/全能小网民
  questionIds: string[] | null  // 终极关为 null，运行时随机
}
```

### 4.3 存档 `localStorage`（key: `huaiya-save`）

```ts
interface SaveData {
  version: 1                 // 存档版本，未来加字段时迁移用
  clearedLevels: number[]    // 已通关关卡 id
  bestScores: Record<number, number>  // 各关最高分（答对题数）
  soundOn: boolean           // 音效开关，默认 true
  nickname?: string          // 证书昵称
}
// 总积分不单独存储，展示时 = Σ bestScores（重玩只抬高不膨胀，杜绝刷分）
```

## 五、核心交互细节

### 5.1 答题反馈（零挫败设计）

- **答对**：鼓励动画（星星/彩带 CSS 粒子）+ 上行音效 + 鼓励文案（随机从鼓励语池取），自动/点击进入下一题。
- **答错**：温和提示动画 + 低柔音效 + 题目原图变暗高亮正确项 + 通俗解析文案，点「我知道了」进入下一题。无批评话术。

### 5.2 解锁与重玩

- 关卡顺序解锁：通关第 N 关解锁第 N+1 关；四关全通后解锁终极关。
- 已通关关卡可重玩，重玩刷新该关最高分与总积分，不重复发勋章。

### 5.3 知识小结

每关结算页自动展示本关 4 题解析的精简版知识点列表（来自题库 `explain` 字段汇总）。

## 六、美术与配图

- **UI 风格**：CSS 手绘卡通风，马卡龙明亮柔和配色（粉/鹅黄/薄荷绿/天蓝），Q 版圆角、大按钮、无刺眼配色。全部 CSS 实现，无 UI 框架依赖。
- **题目主图**：16 张，统一 4:3 横图（SVG viewBox 1024×768），扁平化 Q 版卡通。首发为手绘 SVG 插画；如需更精细的 AI 图，按 [配图清单](./配图清单.md) 生成同名 PNG 放入 `assets/images/` 即自动替换（数据层不存扩展名，`import.meta.glob` 按题目 id 前缀解析，png 优先）。
- **反馈环节**：不单独配图，复用题目原图 + CSS 动画与表情图标。

## 七、音频（真实音频文件）

`src/audio.ts` 播放 `assets/audio/` 下的音频文件（mp3→ogg→webm 优先级，小文件被 Vite 内联为 data URI 不影响播放），命名约定与最小必需集见 `assets/audio/README.md`。

| 音效 | 素材 | 现状 |
|---|---|---|
| 通用点击 | 复用 `select.mp3` | 点击音复用选中音 |
| 选中选项 | `select.mp3` | ✓ |
| 答对 | `correct.mp3` | ✓ |
| 答错 | `wrong.mp3`（暂为 click 音充当，更柔和版后续同名覆盖替换） | ✓ |
| 单关通关 | `victory.mp3` | ✓ |
| BGM | `bgm.mp3`（须能无缝循环） | ✓ |

机关：联动首页/闯关大厅音效总开关（静音即停、开启即恢复）。浏览器自动播放限制：BGM 在首次用户点击手势后启动（`main.ts` 首次 pointerdown 时 `syncBgm()`）。

浏览器自动播放限制：AudioContext 在首次用户点击后 resume。

## 八、通关证书

Canvas 2D 绘制：马卡龙卡通边框 + 5 枚勋章图形 + 称号「青少年健康上网小能手」+ 昵称（默认小槐芽）+ 日期。`toDataURL('image/png')` 触发下载。昵称仅存本地。

## 九、目录结构（规划）

```
huaiya-net-camp/
├── index.html
├── assets/
│   └── images/          # 40 张题目主图 q{关}-{题}.webp（AI 图压缩产物；源 PNG 在 images-raw/ 不入库）
├── src/
│   ├── main.ts          # 入口 + hash 路由
│   ├── styles/          # 全局样式、主题变量
│   ├── data/            # questions.ts / levels.ts
│   ├── audio.ts         # WebAudio 音效
│   ├── storage.ts       # localStorage 存档
│   ├── cert.ts          # 证书 Canvas 绘制
│   └── views/           # home / hall / level / result / medals / cert 各视图
├── docs/
├── README.md
└── CLAUDE.md
```

## 十、开发节奏（三步，每步交用户本地浏览器验证）

1. **骨架 + 全流程**：Vite 脚手架、路由、题库数据、答题页全部题型、判定与解锁逻辑、结算页（SVG 占位图）。
2. **体验完善**：答题反馈动画、WebAudio 音效、勋章中心、知识小结、证书下载、存档。
3. **打磨上线**：响应式细节（手机/平板）、AI 配图接入替换（✅ 已完成：40 张 WebP）、README 完善、Vercel 部署 + Cloudflare 子域 `huaiya.535334587.xyz`。

## 十一、部署

- 仓库：GitHub public 仓库（本仓库）。
- 构建：`vite build`，`vite.config.ts` 设 `base: './'`（相对路径，任意子路径可托管）。仅在线部署。
- 托管：Vercel 上传式部署（login → link → deploy --prod），Cloudflare 添加 CNAME `huaiya` → Vercel 域名。
- 同步原则：推送 GitHub 后提醒是否同步 Vercel，不自动部署。
- **现状（2026-08-29）**：已上线，正式域名 `https://huaiya.535334587.xyz`（Cloudflare CNAME `huaiya` → `8f9d90c8b840bfa2.vercel-dns-017.com`，DNS-only 灰云）。GitHub 仓库已连接 Vercel，**push 到 `main` 即自动部署生产**，无需手动 deploy。

## 十二、待办与遗留

> 记录到 2026-08-29 为止仍未决/未做的项，供后续会话接续。已完成项不再罗列。

### 已完成

| # | 项 | 状态 | 接手提示 |
|---|---|---|---|
| 1 | **音频方案已定：真实音频文件** | ✅ 已启用 | 2026-08-30 用户提供素材并定案：`assets/audio/` 真实文件播放（`src/audio.ts` 已重构，见第七节）。已同步 CLAUDE.md / README 旧「WebAudio 合成」描述。遗留：`wrong.mp3` 暂用 click 音充当、BGM 循环接缝待实听，素材非最终版，后续有更好资源可同名覆盖替换。 |
| 2 | **键盘/读屏可达性 (a11y)** | ✅ 已完成 | 2026-08-30 完成（阶段1 review 建议 #5）：`main.css` 全局 `:focus-visible` 键盘焦点环；大厅关卡卡片加 `tabindex=0`+`role=button`+`aria-disabled` 并支持 Enter/Space 进入；答题配图 `alt` 改为题干主题描述（`level.ts`）。 |
| 3 | **Vercel GitHub 自动部署** | ✅ 已完成 | 已连接，push 到 main 自动上线（本条仅存档）。 |

### 待办候选（本轮未做，后续可续）

> 2026-08-30 收束为 demo：功能闭环已完整上线，以下为梳理出的后续打磨候选，暂不实施。

| # | 项 | 优先级 | 接手提示 |
|---|---|---|---|
| 1 | **勋章图形化** | 低（搁置） | 5 枚勋章现为 `medalEmoji`（🔍/🧼/⏳/🛡️/🏆），可替换为手绘 SVG 徽章，涉及大厅/勋章中心/结算页/证书四处。原计划中已被用户连轮排除。 |
| 2 | **音频终版替换 / BGM 循环实听** | 中 | 直接同名覆盖 `assets/audio/` 下文件即可，代码零改动；替换后需实测 BGM（1.1MB）循环接缝、wrong 音是否够柔和。 |
| 3 | **答题中途退出保护 / 进度接续** | 中（推荐） | 现离开答题页（← 或刷新）会整关丢弃，重进须重来。对中途切换/被打断的小学生不友好。方案：localStorage 暂存当前关卡进度，二次进入可选「继续上次」。（现状已保证未通关不记分、切 hash 即无损放弃，见 result.ts 注释） |
| 4 | **通关庆祝动画（结果页）** | 中 | 结算页现为静态卡片，无答题页那种星星/彩带动效。给通关加粒子 + 勋章弹跳进场，只动 `result.ts` + `main.css`，最小成本。 |
| 5 | **首页首次导览** | 低 | 新用户无玩法说明，仅三行 home-feat。可加「这是什么 + 怎么玩」可展开引导卡（localStorage 判断首次访问）。 |
| 6 | **题目图加载体验** | 低 | WebP 图无占位，网慢时排版跳动。可加占位背景 + 淡入。 |

> 完结注记：2026-08-30 收束为 demo，功能闭环（答题/结算/勋章/证书/音频/a11y）均上线，`huaiya.535334587.xyz` 由 push to main 自动部署。后续按上表候选继续打磨。
